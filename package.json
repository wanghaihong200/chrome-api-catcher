{
  "name": "chrome-api-catcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:css": "tailwindcss -i ./src/manage/tailwind-input.css -o ./src/manage/tailwind.css --minify"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "playwright": "^1.60.0",
    "tailwindcss": "^3.4.0",
    "vitest": "^1.6.0"
  }
}
