# 隐私政策(API Catcher)

最后更新:2026-07-27

## 数据收集

**API Catcher 不收集、不上传、不传输任何用户数据到任何服务器。**

本扩展仅在用户**主动开启录制**时,在本地捕获用户当前浏览页面发出的 HTTP 请求(fetch / XMLHttpRequest),用于接口调试。所有捕获的数据**仅存储在用户本地浏览器的 IndexedDB 中**,不会离开本机。

## 存储的数据(仅本地)

当录制开启时,扩展会在本地存储以下请求信息:

- 请求 URL、方法(GET/POST 等)、请求头、请求体
- 响应状态码、响应头、响应体
- 请求时间、耗时、来源标签页 URL
- 二进制请求/响应体以 base64 编码存储

这些数据**完全保存在本地浏览器**,用户可随时在管理页查看,或通过卸载扩展 / 清除浏览数据删除。开发者无任何途径访问这些数据。

## 数据使用

捕获的数据仅用于:

- 在扩展管理页展示,供用户调试接口
- 生成 cURL 命令(复制到剪贴板,由用户自主操作)
- 导出为 Postman / JMeter / HAR / JSON 文件(由用户主动触发,保存到本地)

## 权限说明

| 权限 | 用途 |
|---|---|
| `storage` | 在本地 IndexedDB / chrome.storage 保存捕获的请求与录制开关状态 |
| `scripting` / `activeTab` / `tabs` | 在用户开启录制的标签页注入抓取脚本、读取标签页信息 |
| `<all_urls>`(host_permissions) | 抓取用户在任意网站发起的接口请求(仅在录制开启时) |

**录制默认关闭**(全局开关与每个标签页开关均默认 OFF,白名单心智)。扩展不会在用户不知情的情况下抓取任何请求。

## 第三方服务

本扩展**不接入任何第三方分析、追踪、广告服务**,不含任何第三方 SDK,不向任何外部地址发送数据。

## 数据安全

捕获的请求可能含敏感信息(Cookie、Authorization 等)。扩展提供:

- cURL 复制时敏感头可一键隐藏(默认展示,可切换)
- 数据仅本地存储,不外传

用户应自行注意不要将含敏感信息的导出文件/cURL 分享给他人。

## 联系方式

如有隐私问题,请在 [GitHub 仓库](https://github.com/wanghaihong200/chrome-api-catcher) 提 issue。
